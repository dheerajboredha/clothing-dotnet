SixteenClothing ASP.NET Core 9 MVC project (auto-generated)
===========================================================

Location: /mnt/data/SixteenClothing_dotnet9

How to build & run (locally):
1. Install .NET 9 SDK from https://dotnet.microsoft.com/download/dotnet/9.0
2. Open terminal and run:
   dotnet restore
   dotnet build -c Debug
   dotnet run

The compiled DLL will be in:
   /mnt/data/SixteenClothing_dotnet9/bin/Debug/net9.0

To publish for deployment:
   dotnet publish -c Release -o ./publish

Notes:
- This environment only generated files. I could not run dotnet build here.
- Security: HTTPS redirection, HSTS and common security headers are added in Program.cs.
- No authentication was added as requested.
